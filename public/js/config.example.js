/* =============================================================
   config.example.js — Template de credenciales de MyPump
   COPIAR este archivo como config.js y completar los valores.
   config.js está en .gitignore — NUNCA se commitea.
   ============================================================= */

window.MYPUMP_CONFIG = {
  SUPABASE_URL:     'https://gydinputrtptqakdzyvc.supabase.co',
  SUPABASE_ANON_KEY: '', // pegar anon public key: dashboard → Settings → API → anon public
};
