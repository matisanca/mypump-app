{
  "name": "mypump-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node scripts/test-bridge.mjs && node scripts/test-outbox.mjs && node scripts/test-wearables.mjs && node scripts/check-contratos.mjs && node scripts/check-healthkit-tipos.mjs && python3 scripts/test_consolidar_identidad.py && python3 scripts/test_centinela_guardrail.py",
    "dev": "npx serve public -l 3000",
    "format": "echo 'TODO: configurar prettier'",
    "ios:add": "npx cap add ios",
    "ios:sync": "npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.1",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/local-notifications": "^8.2.1",
    "@capacitor/push-notifications": "^8.1.2",
    "@capgo/capacitor-health": "^8.9.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
